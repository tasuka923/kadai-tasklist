<hi>
  タスク<%= @task.id %>の編集ページ
</hi>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %><p></p>
  <%= f.text_field :content %><p></p>
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', task_path %>